<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<TargetFramework>netstandard2.0</TargetFramework>
	</PropertyGroup>
	<!--<Target Name="BuildFrontend" BeforeTargets="Build">
		<Exec Command="npm run build" WorkingDirectory="$(ProjectDir)" ConsoleToMSBuild="true" />
	</Target>
	<Target Name="RunFrontend" AfterTargets="Build" Condition="'$(Configuration)'=='Debug'">
		<Exec Command="npm run serve" WorkingDirectory="$(ProjectDir)" />
	</Target>
	<ItemGroup>
		<None Include="**/*" Exclude="node_modules/**/*;dist/**/*" />
	</ItemGroup>-->
</Project>